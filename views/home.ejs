<%- include('partials/header'); -%>
    <h1> Home </h1>
    <p>
        <%= Home %>
    </p>

    <% Post.forEach(element => {%>
        <h1><%= element.title %></h1>
        <p><%= element.post.substring(0,60) %> ....  <a href="/post/<%= element.title %>">Read More</a></p> 
        <%  }); %>

         


<%- include('partials/footer'); -%>